@model NewsArticleModel

@{
	ViewData["Title"] = Model.Name;
}



<div class="container push text-center">
	<img class="img-fluid mt-5" src="@Model.ImageURL" alt="Главна картинка на поста." />
	<h1 class="display-5 mt-3">@Model.Name</h1>
	<p class="mt-3">@Model.Text</p>
</div>
